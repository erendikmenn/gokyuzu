node_modules/
.DS_Store
*.png
!docs/*.png
!/favicon-180.png
!/apple-touch-icon.png
!/apple-touch-icon-precomposed.png
.venv/
__pycache__/
*.pyc
# generated / downloaded (reproducible from tools/ and blender/ scripts)
/assets/
/renders/
data/sf/raw/
data/sf/cache/
*.blend1
# Blender build intermediates (regenerated by the build scripts)
blender/**/build/
blender/**/tex/
blender/**/*.jpg
blender/**/*.exr
*.blend
dist/
# downloaded reference photos/drawings (reference only, never shipped or committed)
blender/**/ref/**/*.webp
blender/**/ref/**/*.jpeg
blender/**/ref/**/*.gif
blender/**/ref/**/*.pdf
blender/**/ref/**/*.tif
blender/**/ref/**/*.tiff
blender/**/refimg/
data/analytics/
# local caches (tools/serve.mjs --assets-from downloads go to .cache/assets/), tool caches, partial downloads
.cache/
.ruff_cache/
data/*/_cache/
data/*/cache/
assets/**/_cache/
*.part
*.log
# local secrets and deploy config: they live in ~/.config (tools/deploy/deploy.env.example is the committed template)
.env
*.env
# internal open-source preparation notes: never committed (only the drafts that become LICENSE / NOTICE / README etc.)
/docs/opensource/*
!/docs/opensource/*.draft.md
