// STUB (aircraft agent replaces): placeholder scaled Cessna until the Blender model exists.
import { stubRig } from '../_stub_rig.js';
export const model = { url: null, lodUrl: null, displays: {}, thumbnail: null };
export function createRig(gltfScene) { return stubRig(1.3); }
